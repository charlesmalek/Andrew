{
  "version": 2,
  "buildCommand": "cd andrew && npm run build",
  "outputDirectory": "andrew/dist",
  "installCommand": "cd andrew && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
